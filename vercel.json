{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "npm install typescript @types/react @types/node && npm run build",
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1"
  }
}
